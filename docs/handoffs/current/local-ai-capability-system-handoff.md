# 本地 AI 能力系统交接

| 字段 | 内容 |
| --- | --- |
| 状态 | M1 基础设施与 A君 live 完成；4070 增强节点待可确认连接信息 |
| 创建时间 | 2026-08-04 Asia/Shanghai |
| 交出者 | Codex |
| 接手者 | A君 / 本机运行验收者 |
| 关联任务 | [架构](../../architecture/local-ai-capability-system.md)、[验收](../../reviews/local-ai-capability-system/acceptance.md) |
| 截止条件 | M1 launchd、A君 4321 与本地 AI 网关重启恢复、完整能力状态回读通过；未配置增强项保持失败关闭 |

## 1. 接手目标

- 目标：让 Mac 在无台式机时独立覆盖方案中的离线核心能力，并向 A君提供稳定能力适配器。
- 用户约束与不可做事项：不停止既有 18080；不读取真实微信；不调用付费或外部 Provider；不把底层资源锁做成第二套 Paperclip 队列。
- 做完的定义：18081/18082 由 launchd 守护，重启后健康；十一项本地模型/知识能力有 E2E 状态；A君私聊默认模型为回环 Qwen3.5；台式机与外部能力边界如实显示。
- 唯一下一步：取得 4070 Ti Super 台式机的可确认内网地址、系统和登录/服务方式，在该明确目标上部署同契约增强服务并做断线回退演练。
- 允许继续的前提：不得猜测局域网邻居身份或向未知设备安装；跨设备发送测试输入仍须逐请求显式批准。

## 2. 当前事实

| 类别 | 事实 | 证据位置 | 状态 |
| --- | --- | --- | --- |
| 代码与文档 | 统一 FastAPI 网关、检索引擎、launchd 配置、A君 OpenAI-compatible 私聊适配已实现 | `integrations/local-ai/`、`ops/local-ai/`、A君相关源码 | 已验证 |
| 本地运行时 | LaunchAgent 18081/18082 健康，十一项本地模型/知识能力、取消与重启恢复已实跑 | 验收账本、`work/local-ai/artifacts/`、`work/local-ai/indexes/`、launchctl/PID 回读 | 已验证 |
| A君 live | 4321 从 source `087fd39…` 的不可变 release `6a41ec6a…` 运行；强制重启后回读本机 AI 与微信只读均为 ready | PID/cwd/entrypoint、`/api/overview`、release 冻结验收 | 已验证 |
| 外部平台 | 台式机地址未配置；Agnes 未调用 | 能力状态 | 未验证且不冒充完成 |
| 人工确认 | 图片输出已视觉检查；未读取私人数据 | 本地图片产物 | 部分确认 |

## 3. 变更与决策

- 已完成：模型隔离环境、文本/视觉、视频抽帧、ASR/TTS、Embedding/Rerank、版本化知识索引与权限过滤、图片三路径、统一状态、串行资源门、取消和受审批的私网台式机转发契约。
- 已完成 4070 软件端：跨机附件与产物传输、Bearer 鉴权、Mac IP allowlist、单 GPU 队列、ComfyUI FLUX.2 adapter、Windows/Linux 安装脚本和可复制 bundle；模拟双进程链路与断线 Mac 默认路由已通过。
- 关键文件或外部配置位置：模型和环境在 `work/local-ai/` 与 Hugging Face 缓存；LaunchAgent 模板在 `ops/local-ai/`。
- 已确定的边界与兼容性约束：18080 保留为显式候选；视频生成联网且需授权；CustomVoice 不冒充声音克隆；桌面节点未配置时 Mac 独立执行。
- 不要重复创建的产物：第二套模型网关、第二套业务任务队列、另一份相同 FLUX 权重或另一套 ASR 默认模型。

## 4. 验证账本

| 层级 | 结论 | 命令或证据 | 未证明部分 |
| --- | --- | --- | --- |
| 自动化 | PASS | local-ai 8/8；A君本机 AI 客户端 3/3；私聊与 task-service 定向 132/132；根 npm test/check；architecture ok | 无本轮已知失败 |
| 运行时 | PASS（M1 + A君 + 跨机协议） | LaunchAgent 18081/18082 健康；十一项本地模型/知识能力 E2E；4321 新 release；模拟 4070 鉴权、附件、产物与断线 Mac 路由 | 真实 4070 CUDA/FLUX 未接入 |
| 外部平台 | NOT CHECKED | 未调用 | 4070 节点与 Agnes |
| 人工验收 | PARTIAL | 三张 FLUX 输出已视觉检查 | 用户最终使用体验 |

## 5. 风险、权限与关闭

- 当前阻塞或风险：4070 台式机没有可确认的内网身份或连接入口；局域网只读发现未找到开放的 SSH、SMB、RDP 或 AI 服务端口，不能对未知邻居执行安装。
- 不得复制或展示的信息：真实微信聊天、联系人标识、Cookie、token、模型 Provider 凭据。
- 需要谁确认：只有真实微信内容验收、台式机内网地址或外部视频生成需要用户另行授权/提供。
- 关闭条件：M1 基础设施与 A君 live 已满足；若把“整个系统”定义为包含 4070 增强，则还需台式机内网地址、系统和可用登录/服务入口。未授权外部能力继续明确显示未配置。
- 关闭证据链接：A君证据已补到本交接和验收账本；4070 完成后继续补同一账本。
