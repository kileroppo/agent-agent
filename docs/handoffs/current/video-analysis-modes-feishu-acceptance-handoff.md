# 视频分析四模式飞书验收交接

| 字段 | 内容 |
| --- | --- |
| 状态 | 待修复后真实飞书复验 |
| 创建时间 | 2026-08-07 21:49 CST；2026-08-08 09:18 更新连续失败修复 |
| 交出者 | Codex |
| 接手者 | 负责人 |
| 关联任务 | [M5 验收记录](../../reviews/m5-high-autonomy-content-operations/acceptance.md#2026-08-07-视频分析四模式单版本验收) |
| 截止条件 | A君 真实飞书原会话完成一条指定模式的视频分析并返回可核对报告 |

## 1. 接手目标

- 目标：连续失败修复已经完成，只补一条修复后的真实飞书入口验收。
- 用户约束与不可做事项：只使用公开或已授权视频；不启用 Campaign、Cron、Publisher，不发布内容，不自动创建小创、审核或发布任务。
- 做完的定义：飞书原会话返回任务结果，任务输入保留指定 `analysisIntent`，报告含 `reportVersion=video-analysis/v2`、来源证据和该模式唯一下一步。
- 唯一下一步：负责人在 A君 真实飞书私聊重新发送一次“请用精华提炼分析这个视频：<同一公开或已授权视频链接>”；系统应复用已有小D来源产物，只新增小拆分析任务。
- 允许继续的前提：视频可合法读取；若使用未确认机器稿，只能接受初步摘要，不进入小创。

## 2. 当前事实

| 类别 | 事实 | 证据位置 | 状态 |
| --- | --- | --- | --- |
| 代码与文档 | 四模式、入口透传、来源复用、证据门禁、指标学习建议和安全回退已实现 | 关联验收记录 | 已验证 |
| 本地运行时 | 已从干净源码冻结不可变 release 并完成 launchd、端口、cwd 与真实 DeepSeek 任务验收；当前值需重新读取 | launchd、release manifest 与 `/api/overview` | 已验证 |
| 外部平台 | 负责人已真实发送两次；两次均因 `visualMode=auto` 被错误当成“必须有受控视觉观察”而失败。修复后尚未发送复验消息；M5 Campaign、每日入口 Cron 和 Publisher 仍保持关闭 | 任务 `#66C3F7F9`、`#336712B6` | 部分验证 |
| 人工确认 | 负责人尚未核对真实飞书结果呈现 | 本交接单 | 待确认 |

## 3. 变更与决策

- 已完成：结构化模式优先、自然语言冲突提示、四类报告、来源产物复用、确认稿与证据校验、人工进入小创门禁、指标样本门禁、Hermes 外层失败的本机证据回退。
- 关键文件或外部配置位置：`apps/ajun-runtime/src/analysis-intent.js`、`apps/ajun-runtime/src/local-content-growth.js`、`apps/ajun-runtime/src/paperclip-hermes-task-reconciler.js`、`agents/video-content-analyst/`。
- 已确定的边界与兼容性约束：旧 `depth` 继续兼容；一个任务只执行一种模式；深度确定性回退只进入待测试；创建、审核、发布保持分离。
- 不要重复创建的产物：已有来源任务 `c0636161-cb44-4449-81ee-9baa4e027570`；安全回退验收任务 `7d45ed66-e86a-4a08-8179-509939352593`；真实模型结果页验收任务 `1f5d2f8a-ab1f-4ee3-aeb8-e44124743867`。

## 4. 验证账本

| 层级 | 结论 | 命令或证据 | 未证明部分 |
| --- | --- | --- | --- |
| 自动化 | PASS | A君 `1254/1254`；视觉自动降级与明确必看画面的反向门禁通过；A君错误归因规则 `19/19` | 无修复后的真实飞书消息 |
| 运行时 | PASS | release `73612393bf5f…` 已加载；A君 PID `75211`、Hermes PID `75891`；同一小D确认稿和故事板只读复演返回 `succeeded/digest/video-analysis/v2/partial` | 修复后的真实飞书入口仍未验收 |
| 外部平台 | PARTIAL | 两次真实飞书入站均已保留失败事实；修复没有创建第三个任务，任务总数保持 `789`；发布相关入口保持关闭 | 需要负责人发送一条修复后复验消息并核对原会话回传 |
| 人工验收 | NOT CHECKED | 无 | 负责人需检查报告可读性、模式和唯一下一步 |

## 5. 风险、权限与关闭

- 当前阻塞或风险：两次历史失败不会自动恢复；真实根因是 `controlled_provider_vision_required`，不是“本机分析模型服务未启动”。`visualMode=auto` 现在会如实交付部分文字报告，只有 `visualMode=required` 仍阻断。剩余阻塞只有负责人发送一次修复后消息并检查原会话结果。
- 不得复制或展示的信息：Hermes/飞书凭据、Cookie、授权链接、私密视频内容和本机敏感路径。
- 需要谁确认：负责人。
- 关闭条件：真实飞书任务保留指定模式并在原会话返回证据化报告；没有新增素材处理、小创、审核或发布任务。
- 关闭证据链接：验收后补到本文件和关联验收记录。
