# 能力 Plan B 与运行事件交接

| 字段 | 内容 |
| --- | --- |
| 状态 | 待验收 |
| 创建时间 | 2026-08-13 Asia/Shanghai |
| 交出者 | Codex |
| 接手者 | Agent军团集成负责人 |
| 关联任务 | [能力 Plan B 与运行事件架构](../../architecture/capability-routing-and-run-events.md) |
| 截止条件 | 独立干净候选完成不可变发布、运行台回读和任务时间线验收 |

## 1. 接手目标

- 目标：把本次已验证候选安全收进不可变运行版本，并完成任务详情时间线验收。
- 用户约束与不可做事项：不复制控制面；不盲目重试；不读取或记录凭据；结果未知不重复外写；不覆盖当前工作树中其他任务的飞书卡片改动。
- 做完的定义：候选在干净来源通过架构门禁并发布；真实任务详情显示按顺序落账的运行事件；至少一条已登记能力证明成功或安全 Plan B；人工确认界面信息层级可用。
- 唯一下一步：从当前已通过全仓回归的完整工作面形成独立 clean 候选，冻结并切换 A君与小D；不得从旧 HEAD 单独抽 Plan B 而回退已在线的飞书任务卡行为。
- 允许继续的前提：切换前再次确认无执行中任务和待审批任务；候选通过全仓测试、TypeScript、架构边界和差异检查。

## 2. 当前事实

| 类别 | 事实 | 证据位置 | 状态 |
| --- | --- | --- | --- |
| 代码与文档 | Route/Plan B、Receipt v2、事件存储、时间线、ASR/采集/视觉/本机AI/StepFun/飞书交付事件，以及独立复核和两轮返工已实现 | 本文关联架构文档及对应源码 | 已验证 |
| 本地运行时 | 临时 A君 4399 启动成功；创建任务后时间线回读 `task_received → delivery_brief_resolved` | 本次验证账本 | 已验证 |
| 外部平台 | `https://example.com/` 真实公开正文读取通过质量门并记录策略/成功事件；未执行外部写入 | 本次验证账本 | 部分验证 |
| 人工确认 | 任务详情时间线尚未由负责人浏览器验收 | 待负责人检查 | 待确认 |

## 3. 变更与决策

- 已完成：主路线/最多两备路线、确定失败/结果未知分流、预算和权限边界、90 天事件明细与跨周期永久事故摘要、任务详情时间线、ASR、MediaCrawler→yt-dlp、公开网页、本机 AI、StepFun 和飞书交付接线；重要任务自动独立复核、证据绑定与最多两轮返工。
- 关键文件或外部配置位置：`apps/ajun-runtime/src/workflow/`、`apps/ajun-runtime/src/task-run-event-*`、`apps/ajun-runtime/src/task-timeline-*`、`apps/xiaod-media-transcriber/src/asr-capability-adapter.js`。
- 已确定的边界与兼容性约束：旧单 Adapter API 保留；事件账本独立于任务库；日志失败不得改变业务结果；外写 ambiguous 不切换。
- 不要重复创建的产物：任务库、队列、调度器、独立日志控制台、公共网页客户端和 ASR 客户端。

## 4. 验证账本

| 层级 | 结论 | 命令或证据 | 未证明部分 |
| --- | --- | --- | --- |
| 自动化 | PASS | 2026-08-13 根 `npm test --silent` exit 0；小D 89/89；A君 TypeScript、根架构门禁和 diff check 通过 | 不替代真实 Provider 与人工采用 |
| 运行时 | PASS | 临时 4399 启动、任务创建、时间线 API 与静态模块回读成功 | 未切换正式 4321 release |
| 外部平台 | PARTIAL | 真实公开网页读取成功；未发外部消息、未写文档、未产生付费调用 | 图片生成和具体外部文档 Provider 未登记真实路线 |
| 人工验收 | NOT CHECKED | 无 | 浏览器信息层级、窄屏和真实飞书入口仍待人工确认 |

## 5. 风险、权限与关闭

- 当前阻塞或风险：正式 4321 与 4318 仍是旧进程；必须从完整当前工作面形成 clean 候选后再切换。不是所有能力都有第二 Provider；无等价备用路线时按契约安全停止并留回执。
- 不得复制或展示的信息：任何 `.env`、Token、Cookie、授权链接、Prompt 或任务正文。
- 需要谁确认：飞书任务卡改动所有者先收口；负责人完成任务详情人工验收。
- 关闭条件：干净不可变 release 的指纹、HTTP 回读、真实任务事件回执和人工界面验收均入账；未接真实 Provider 的能力继续明确标记未接入。
- 关闭证据链接：待发布后补充到本交接单。
