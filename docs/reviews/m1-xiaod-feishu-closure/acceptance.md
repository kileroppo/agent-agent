# M1 小D飞书受控验证记录

| 字段 | 内容 |
| --- | --- |
| 状态 | 部分通过：短媒体闭环、确定性状态查询、终态文档回传、人工可读性、真实失败恢复及长媒体真实阶段/交付已验证；待其余M1场景 |
| 日期 | 2026-07-19 |
| 验收范围 | 隔离 Hermes Profile、飞书测试应用、最小事件与发布边界 |
| 不包含 | 真实 App Secret、用户标识、模型密钥、真实媒体任务 |

## 本次变更

- 创建本机隔离 Hermes Profile：`xiaod`；
- 不克隆默认 Profile，不切换默认 Profile；Gateway 仅在本次受控验证中以独立 Home 前台启动；
- 创建并发布飞书个人版测试智能体应用；
- 机器人能力设为移动端与桌面端默认能力；
- 事件接收方式为长连接；
- 仅保留 `im.message.receive_v1`；
- 删除群成员、消息表情、文档评论、会议、纪要和妙记等超出 M1 的预置事件；
- 关闭外部群使用和外部用户单聊；
- 在所有者完成专用本地配置后，以独立 Hermes Home 启动小D前台 Gateway。
- 创建并发布传统飞书企业自建机器人应用“小D · M1机器人测试”；
- 新传统机器人仅启用机器人、长连接、应用身份 `im.message.receive_v1`、`im:message` 与读取用户发给机器人的单聊消息所需权限；
- 新传统机器人关闭外部群和外部用户单聊。

## 已验证

### 本地

- `hermes profile show xiaod` 确认隔离 Profile 已创建，Gateway 状态为 stopped；
- 小D Manifest、Prompt、评测和 Hermes 映射的契约测试 3/3 通过；
- 映射文件不保存 secret，Profile 仍为 `draft`。
- 发现 `HERMES_PROFILE=xiaod` 不足以让通用 Hermes CLI 进入独立 Home；后续启动与验收必须显式使用小D Profile 目录，避免误读默认环境。
- 所有者已确认隔离 Profile 的飞书白名单、飞书凭据与专用模型配置完成；本次未读取 `.env` 内容。
- 小D Gateway 已以独立 Home 前台运行，安全日志摘要显示飞书长连接已建立，未发现错误标记；未发现入站消息标记。
- 在一键智能体应用的一次测试中未观察到入站标记后，已停止该隔离前台 Gateway；具体原因尚未定位，默认 Hermes Gateway 未受影响。

### 飞书外部平台

- 飞书开发者后台确认测试应用版本 `1.0.1` 已发布并审核通过；
- 机器人能力已启用，移动端与桌面端默认能力均为机器人；
- 事件列表只保留 `im.message.receive_v1`；
- 发布版本中，外部群和外部用户单聊均为不允许；
- 应用 Secret 始终保持掩码，未读取、复制或记录。

## 未验证

- 隔离 Profile 的传统机器人凭据已由所有者配置；本次未读取 `.env`，因此仅以所有者确认作为该配置事实的依据；
- 已验证消息身份准入与普通文本回复；未验证小D业务任务创建、卡片回调或业务产物状态回写；
- 未创建 Hermes Kanban 任务，未验证幂等、重试、取消或恢复；
- 未创建飞书文档，未验证写入、权限授予和接收人读取；
- 未运行真实短素材任务，未验证小D产物闭环；
- 飞书一键创建向导使用了账户默认的应用名称，未采用测试名称；名称不影响当前最小接入验证，但在人工测试前应由所有者确认或更名。

## 本轮消息冒烟（未通过）

- 时间：2026-07-19 10:32 Asia/Shanghai；
- 所有者已在飞书客户端向当前测试机器人发送无敏感测试消息；
- 隔离 Gateway 进程仍在线，飞书长连接仍存在，安全日志摘要未发现错误；
- Gateway 日志未出现入站事件、白名单拒绝、任务创建或回复标记，因此不能证明消息已到达小D；
- 飞书客户端截图已确认会话确为当前测试智能体，且消息发送成功；
- 飞书官方最新资料说明：一键创建的智能体应用可用于 Hermes/OpenClaw 接入。当前只可确认该次测试未出现安全日志的入站标记，具体原因尚未验证；
- 已选择并落地传统机器人应用路线，见 ADR-0003。这是缩小 M1 验证范围的选择；传统机器人凭据已配置，下一步是验证消息链路。

## 下一门禁（已由文本链路验证取代）

下一项验证应把飞书消息映射为小D业务任务，而不是继续验证泛化聊天回复：

```text
飞书媒体消息 → 小D任务创建 → 转录/整理 → 飞书文档与消息交付
```

## 传统机器人消息冒烟（未通过，待重试）

- 时间：2026-07-19 11:02 Asia/Shanghai；
- 隔离小D Gateway 使用现有传统机器人配置启动，已建立飞书长连接；
- 所有者确认已发送一条测试消息后，Gateway 连续观察未出现入站、白名单拒绝、任务创建或回复标记；
- 飞书开发者后台已只读核对：传统机器人应用已发布，机器人能力、长连接、`im.message.receive_v1` 与读取用户发给机器人的单聊消息权限均存在；事件日志亦未见该次推送记录；
- 结论：本次消息未进入该传统机器人应用，尚不能判断为 Gateway 或小D业务问题。下一次必须确认会话顶部名称为“`小D · M1机器人测试`”，而非此前的一键智能体会话，然后重新发送一条无敏感测试消息。

### 诊断重试

- 时间：2026-07-19 11:19 Asia/Shanghai；
- 所有者截图确认测试消息发往“`小D · M1机器人测试`”；
- 隔离 Gateway 的脱敏诊断日志确认收到两条入站事件，均为 `dm_policy_rejected`；
- 结论：飞书投递与 Hermes WebSocket 适配器正常，当前阻塞点是 `FEISHU_ALLOWED_USERS` 未匹配发送者的用户 `open_id`。会话 ID（`oc_...`）不能作为该白名单值；更新为获授权测试用户的 `open_id`（通常为 `ou_...`）并重启 Gateway 后重试。

### 白名单修正后的运行时验证

- 时间：2026-07-19 11:28 Asia/Shanghai；
- 所有者更新白名单并重启隔离 Gateway 后，机器人不再出现 `dm_policy_rejected`，并成功向原会话发送了 Home Channel 提示与错误回复；
- 这证明“飞书消息 → Hermes 准入 → 飞书出站回复”已经真实打通；
- 隔离运行时错误日志的脱敏诊断显示：`RuntimeError: No LLM provider configured`。这表示运行时尚未在 Profile 的 `config.yaml` 选择模型 Provider；不读取或记录模型环境变量、密钥、消息正文；
- 同时观察到可选的 `im:chat:readonly` / `im:chat` 权限缺失提示，它影响 Hermes 获取会话信息，但不是本次模型错误的根因；
- 当前阻塞点转为小D专用模型 Provider 的选择与默认模型配置。完成该项后，须从同一会话发送一条新的无敏感测试消息，验证模型正常响应。

### 模型选择后的 Provider 验证

- 时间：2026-07-19 11:39 Asia/Shanghai；
- Profile 已加载 `stepfun` Provider 与模型配置，飞书消息可到达模型调用层；
- Provider 返回 HTTP 401 `Incorrect API key provided`；日志不记录实际 Key 或完整 Provider 地址；
- 结论：模型路由已生效，当前唯一阻塞是 `STEPFUN_API_KEY` 与当前 StepFun 接入地址不匹配。若使用 StepFun 官方直连，应使用 StepFun 平台生成的有效 Key 并移除非必要的 Base URL 覆盖；若使用其他 OpenAI 兼容代理，则必须改用对应的 `openai-api` Provider 配置，不能混用为 `stepfun`。

### Step Plan 文本对话闭环（通过）

- 时间：2026-07-19 Asia/Shanghai（第 8 次无敏感文本消息验证）；
- 所有者确认使用 Step Plan 套餐后，Profile 配置为 Step Plan 中国区地址与 `step-router-v1`；
- 所有者在“`小D · M1机器人测试`”会话发送第 8 条无敏感测试消息，机器人返回正常中文回复；
- 结论：已真实通过“飞书消息 → 白名单准入 → Hermes → Step Plan 模型生成 → 飞书回复”。此结论不等同于小D媒体业务交付完成。

### 飞书媒体任务入口（本地实现与接口冒烟通过）

- 时间：2026-07-19 Asia/Shanghai；
- 小D本地服务新增 `POST /api/internal/feishu-media`：仅接受 Hermes 缓存目录内的音频/视频文件，先复制到小D工作区，再创建任务；同一个缓存媒体路径会复用同一任务，避免重复运行；
- `apps/xiaod-media-transcriber` 自动化测试 12/12 通过；临时空文件的接口冒烟确认首次请求创建任务、重复请求返回同一任务，随后因素材无效而如实失败；该临时文件和临时工作区已移入本机废纸篓；
- 隔离 Gateway 与小D服务已重新启动，飞书长连接已恢复；小D运行时 Prompt 已加入文件任务创建指令；
- 未发送真实飞书媒体，未运行真实 ASR 或飞书文档写入。因此这项是“入口实现通过”，不是媒体交付通过。

该测试通过前，不得把小D标记为 `active`，也不得报告飞书业务闭环完成。

### 首条真实飞书媒体任务（系统交付通过，待人工读取确认）

- 时间：2026-07-19 14:05–14:08 Asia/Shanghai；
- 入口：所有者在“`小D · M1机器人测试`”发送一条短、无敏感的媒体附件（非语音条）；
- 任务：`7df3c85a-3d74-46e1-a364-fb048412cf34`。`GET /api/jobs/<id>` 显示其入口为 `platform: feishu`，并保存 `messageId + attachmentIndex` 幂等键；
- 业务结果：任务依次经过 `queued`、`preparing`、`transcribing`、`distilling`、`delivering`、`completed`，最终状态为 `completed`，无错误或警告；
- 产物结果：原始转录、内容导览、完整校对文本和分享式整理稿均已在任务目录生成且非空；服务记录飞书文档已创建、权限授予成功；
- 运行时：小D服务仍仅监听 `127.0.0.1:4318`；隔离 Gateway 使用 `HERMES_HOME=/Users/pengaro/.hermes/profiles/xiaod` 与本机媒体入口运行；
- 结论：首条“飞书媒体附件 → 唯一小D任务 → 本地转录/整理 → 飞书文档交付”已获得系统层证据，不等同于M1整体通过。

仍待：所有者从机器人交付消息实际打开该文档并确认可读；随后继续验证长任务阶段、重复事件、失败恢复与其他PRD验收项。未完成前，小D仍不得标记为 `active`。

### 终态消息回传缺失（已修复，待真实回归）

- 现象：首条真实任务已在小D服务中完成并生成飞书文档，但机器人仅发送“已创建小D任务”，没有后续交付消息；
- 原因：强制路由适配器创建任务后即返回，未订阅或轮询小D任务的终态；
- 修复：适配器现对每个任务建立受控后台轮询，在 `completed`、`failed` 或 `cancelled` 时向原会话发送一次终态消息；仅当飞书文档存在且权限已确认时才附交付入口，重复事件不会建立第二个通知任务；
- 本地验证：适配器语法检查通过；终态文案与重复通知回归用例 2/2 通过；小D应用测试 12/12 通过；隔离Gateway已重启并建立飞书长连接；
- 未验证：旧任务在修复前已结束，不能证明补丁会补发历史消息。必须用新的无敏感短媒体验证真实“创建 → 终态回传 → 文档可读”路径。

### 终态回传真实回归（系统通过，待人工点击）

- 时间：2026-07-19 14:50 Asia/Shanghai；
- 任务：`41a6d1fb-40a6-421e-9c8a-e33b1bb10c1c`；
- 飞书证据：机器人先回复任务已创建，随后在同一会话发送“已完成”和交付文档卡片；
- 服务证据：任务状态为 `completed`，无错误或警告，阶段完整经过 `queued`、`preparing`、`transcribing`、`distilling`、`delivering`、`completed`；原始转录、内容导览、完整校对文本和分享式整理稿均存在；飞书文档已创建且权限授予成功；
- 结论：已真实验证“创建 → 终态回传 → 飞书文档卡片”路径。仍待所有者点击卡片中的文档入口并确认可读，不能据此宣布M1其余长任务、重复事件和失败恢复场景完成。

### 状态询问误入旧模型会话（已修复并完成真实回归）

- 现象：媒体任务 `347f0169-ccb8-4970-b5e7-5f76725c390e` 已正常完成并创建授权文档，但用户紧接着询问“目前进行得如何”时，通用模型会话错误翻出历史附件并回复模型加载问题；
- 根因：飞书媒体先进入通用媒体批处理，状态文本先于该批次创建小D任务，因而落入旧模型上下文；
- 修复：小D媒体改为在通用批处理之前直接创建任务；常见状态询问改为只读当前会话最近的小D任务，禁止进入通用模型；
- 验证：适配器语法检查通过，媒体绕过批处理、状态询问、终态链接和去重通知回归用例 4/4 通过；隔离Gateway已重启并重新连接飞书；
- 真实回归：2026-07-19 15:01–15:02，所有者授权上传同一无敏感短媒体后创建任务 `b078b4a2-a6fa-466d-af4f-bda1cb5bbd06`，随即发送“目前进行得如何”。飞书先回传该任务的终态文档，再对该询问回复同一任务的确定性完成状态和同一交付入口；未出现通用模型的模型加载或工具执行回复。
- 服务证据：任务状态为 `completed`，无错误、无警告，飞书文档权限授予成功。
- 结论：已真实验证媒体直达小D和状态询问绕过通用模型会话。仍需所有者打开本次交付文档确认可读；不代表长任务、重复事件和失败恢复已通过。

### 交付文档人工读取确认（通过）

- 时间：2026-07-19，所有者确认本次交付文档“勉强能读”；
- 结论：文档入口可打开并可读取，人工可读性门禁通过；“勉强”表示内容质量仍可在后续 refinement/排版迭代中改善，不改变本次交付链路通过结论。

### 重复事件幂等（通过）

- 时间：2026-07-19，使用已完成任务的同一 `messageId + attachmentIndex` 在本地受控入口重放一次；
- 结果：入口返回 `duplicate=true` 并复用原任务 ID；任务仍为 `completed`，没有重新执行，也没有产生第二份任务或交付产物。
- 结论：业务入口幂等通过；适配器终态通知去重另有 4/4 自动化回归覆盖。下一项进入受控失败恢复，不扩大到长任务验证。

### 受控失败回归（失败分类通过，恢复动作已实现、待真实回归）

- 时间：2026-07-19 15:29；入口：飞书测试会话上传明确无效的音频文件；任务 `b4edde5b-b6d7-48d8-96d9-99810dd25eb1`；
- 服务证据：任务进入 `failed`，阶段为“处理失败”，错误明确为“素材无法被识别为可转录的音视频文件，请重新上传原始音频或视频文件。”；无错误文档入口、无伪造完成状态；
- 飞书证据：先收到任务创建消息，再收到“未完成/处理失败”终态；随后询问“目前进行得如何”仍返回同一任务的确定性失败状态；
- 已补齐代码：小D失败记录现在保存 `category`、`retryable` 和面向用户的恢复说明；只有 `failed + retryable` 的任务可经 `POST /api/jobs/:id/retry` 继续，已完成任务、无效媒体和需人工处理任务均拒绝自动重试。飞书同一会话支持“重试小D任务”，并在重试后重新建立一次终态通知；旧失败原因进入尝试记录，不清空已确认产物。
- 自动化证据：小D `npm test` 15/15 通过；飞书适配器受控用例 5/5 通过，覆盖重试命令匹配、仅可重试失败才提示命令、已完成任务不可作为重试候选。
- 未完成的真实证据：尚未运行“可重试失败 → 飞书回复重试 → 从安全断点完成且无第二份交付”的外部回归。因此 US-005 仍为部分通过。

### 后台托管修复后的短媒体回归（通过）

- 时间：2026-07-19 16:53 Asia/Shanghai；所有者在同一小D飞书测试会话重新上传已授权短媒体；
- 飞书证据：机器人立即回复唯一任务已创建，随后在同一会话回传“已完成”和可打开的飞书文档卡片；
- 服务证据：对应任务为 `completed`，无错误、无警告，阶段为“处理与交付均已完成”，飞书文档已生成且权限标记为已授予；
- 运行证据：小D应用和隔离Gateway均由本机服务管理器托管，入口对 Hermes 缓存路径的只读探针返回“文件不存在或已过期”而非“不受信任”，确认启动环境已恢复缓存根目录。
- 结论：此前的“暂时无法接收”是后台托管启动时遗漏 Hermes 缓存根目录导致，不是文件格式或飞书会话问题；修复后短媒体成功闭环再次通过。

### 可重试失败恢复真实回归（通过）

- 时间：2026-07-19 19:03–19:18 Asia/Shanghai；所有者在小D飞书测试会话上传已授权短媒体，应用在 `transcribing` 阶段按一次性本机验收开关受控失败；该开关随后已关闭。
- 失败证据：任务 `6370def3-9fb0-4ed8-9cb1-eecad5b7465a` 进入 `failed`，明确标记为 `retryable`，未创建飞书文档或本地交付物。
- 路由修复：首次 @机器人 重试文本因飞书规范化出的前置 @ 提及未匹配而误入通用模型，出现命令授权卡；已修复确定性匹配并新增回归用例。该命令未获批准、未执行工具。Gateway 重启后，带明确任务号的重试命令被小D直接接收。
- 恢复证据：同一任务从安全断点重新进入队列，最终为 `completed`；任务保存一条 `retry` 尝试及原失败原因，日志保持“失败 → 安全重试 → 处理与交付完成”的完整顺序。
- 交付证据：服务记录飞书文档存在、权限标记已授予；飞书会话显示一份完成文档卡片和一次完成回传。
- 自动化与运行证据：小D `npm test` 19/19 通过；飞书适配器受控用例 5/5 通过；小D服务监听 `127.0.0.1:4318`，验收开关为关闭状态，隔离 Gateway 正在运行。
- 结论：US-005 的真实可恢复失败场景通过。该场景在交付前失败，因此证明“未确认产物不重复”；长媒体阶段通知与其他M1验收项仍未通过本次回归覆盖。

### 长媒体真实阶段与交付回归（通过）

- 时间：2026-07-19 19:25 Asia/Shanghai；所有者在小D飞书测试会话上传已授权 MP3，任务 `9d6b22ed-d48b-44af-8276-bce90fabed4c`。
- 媒体规模：服务端只读探测为 632.2 秒（约 10 分 32 秒）；不读取或记录媒体内容。
- 服务证据：任务为 `completed`，无错误、无警告、无重试；持久化阶段账本依次为 `queued`、`preparing`、`transcribing`、`distilling`、`delivering`、`completed`。
- 交付证据：本地整理稿存在；飞书文档存在且权限标记已授予。
- 飞书证据：会话显示一条任务创建回复和一条完成文档卡片，没有逐内部步骤刷屏。
- 结论：US-003 的真实阶段、限流回传和长媒体交付通过。后台阶段更新故障后的恢复仍未单独注入验证。
