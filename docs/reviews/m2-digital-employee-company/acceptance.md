# M2 数字员工公司体验验收

| 字段 | 内容 |
| --- | --- |
| 状态 | 三名业务员工与六名治理员工飞书闭环完成；私人云与 Mac 真关机不在当前本地运行范围 |
| 目标设计 | [数字员工公司体验设计](../../design/digital-employee-company-experience.md) |
| 实施计划 | [实施计划](../../plans/m2-digital-employee-company-implementation-plan.md) |
| 最后更新 | 2026-07-27 |

> 只有真实证据才能改为通过。自动测试、当前运行、外部平台和人工可用性分别记录，不互相替代。

| 编号 | 验收项 | 自动化 | 当前运行 | 真实飞书/外部 | 人工可用性 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| EMP-001 | 小D完成真实素材整理并交付可读产物 | 372/372 回归通过 | 真实公开 MP4 完成，质量与可读性通过 | 飞书文档权限已确认 | 待老板阅读 | 本机与产物通过 |
| EMP-002 | 资料研究员完成带来源的真实公开研究报告 | 执行器、路由和呈现覆盖通过 | CERN 公开网页研究成功且产物可读 | 小R独立应用已发布；老板私聊入站、同会话报告回传通过 | 待老板评价结果质量 | 飞书业务闭环通过 |
| EMP-003 | 办公执行助理完成非空可读汇报包 | Manifest、执行器、路由和呈现覆盖通过 | 真实总任务生成非空 Markdown 汇报包 | 小办独立应用已发布；老板私聊入站、同会话汇报回传通过 | 待老板评价结果质量 | 飞书业务闭环通过 |
| EMP-004 | A君一次分派三项业务工作并统一汇报 | 父子任务、并行、延迟汇总、MCP 覆盖通过 | 三员工总任务 3/3 成功并统一汇报 | A君真实飞书入站、自动分工、3/3 最终汇报通过 | 待老板评价产物质量 | 通过 |
| EMP-005 | A君和员工入口读取同一任务与结果 | MCP 作用域和共享任务读取覆盖通过 | A君 Hermes 原生读取同一总任务；员工入口进入同一任务服务与产物记录 | 小R、小办独立入口均完成真实任务 | 待老板确认跨入口体验 | 通过 |
| EMP-006 | 只有缺材料、审批、重要异常、判断和交付时通知 | 终态、进行中、缺材料、失败和短时终态等待覆盖通过 | 总任务等待常见短任务完成后直接返回终态 | 真实飞书先如实报告两次重要异常；修复后仅返回一条 3/3 最终汇报 | 待老板评价汇报简洁度 | 通过 |
| EMP-007 | 低风险自动、高风险拒绝后目标动作不发生 | 审批与拒绝回归通过 | 公开发布任务停在 Paperclip 审批；拒绝后取消、0 子任务、0 产物 | 不适用 | 不适用 | 通过 |
| EMP-008 | 重启后从安全阶段继续且不重复交付 | 任务恢复、幂等和 Gateway 基线通过 | A君与 Hermes 重启后 3/3 结果仍可读；重复请求任务数不变 | 真实飞书跨重启追问命中同一总任务，任务数 `236 → 236` | 不适用 | 通过 |
| EMP-009 | 三名员工使用独立 Hermes Profile、记忆和工具边界 | MCP 硬作用域、Manifest、模型就绪投影、Gateway 唯一接管和接线安全测试通过 | 小D Profile 已运行；小R和小办各自 Profile、模型、SOUL、MCP 与 Gateway 均运行 | 小R和小办均完成独立模型真实调用、真实飞书任务及连续追问 | 已完成直接对话 | 本机通过 |
| EMP-010 | 私人云端办公室在 Mac 关机后仍可接单和做轻量工作 | 云端等待、租约接管、幂等、重启恢复、唯一接管、预检和产物门禁通过 | 隔离云端在无 Mac Worker 时完成小R研究与办公汇报；小D安全等待；重启后全部恢复 | 真实云主机和关机后飞书待验证 | 待确认 | 隔离通过，真实云待验收 |
| EMP-011 | 六名治理员工像独立员工一样拥有 Profile、Skill/MCP、飞书入口与 Paperclip 执行身份 | Manifest 自动发现、Profile 映射、配置器、MCP 作用域、run 身份、飞书验收器与技术修复回执覆盖通过 | 六名员工 Gateway/launchd 运行；六项 Paperclip 岗位任务各 1 次 run 并以员工身份回写；技术修复真实测试与恢复检查通过 | 老板真实逐个私聊六名员工；六个独立会话均回复并命中验收代号；创建官跨 Gateway 重启继续同一会话 | 六份回复已核对岗位边界；一处虚报能力已修复并复验 | 已完成 |
| EMP-012 | A君复用通用账号连接管理，后续员工扩张不增加登录库和模型白名单 | 账号连接脱敏、回环限制、撤销、控制台边界和动态 Profile 解析测试通过 | 正式 `4321` 读取小D连接服务 6 条安全状态；A君 launchd 重启后 API 正常 | 本机真实浏览器显示 6 张状态卡、3 个撤销入口、0 个 Cookie/Token/密码输入 | 页面布局与状态文案已核对；未执行真实撤销 | 本机通过 |

## 验收证据

实施过程中按编号追加时间、任务号、产物引用和脱敏结论。不得记录 App Secret、token、Cookie、用户标识或聊天标识。

### 2026-07-27 六名治理员工独立执行链

- 创建官 `AGE-383` 完成最小岗位草案；任务协调官 `AGE-388` 交付三岗位分工；审核官 `AGE-387` 对完整最小权限草案给出进入负责人决策的建议；架构师 `AGE-396` 交付六员工扩展评估。
- 运维官 `AGE-386` 在没有端口/PID 证据时如实回报 `waiting_test`，没有猜测健康；这属于安全岗位结论，不是执行失败。
- 技术专家 `AGE-393` 通过 A君隔离修理房只修改 `docs/acceptance-fixtures/governance-hermes-technical-canary/calculator.mjs`；`node --test` 为 1/1 通过，`add(2,3)=5` 与 `add(-1,1)=0` 恢复检查通过，随后才安全带回主工程并回报 `succeeded`。
- 六项终验均只产生 1 个 Paperclip run，完成评论均归属对应 `agent`，没有以用户身份回写后再次唤醒。
- 六名员工分别有独立 Hermes Home/Profile/SOUL、岗位 Skill/MCP、Paperclip `hermes_local` Adapter 和 launchd 飞书 Gateway。
- 六名治理员工真实飞书私聊使用统一代号“青松”验收。脱敏验收器确认六个 Profile 各有自己的 Feishu Session、老板消息与员工回复均命中代号、会话指纹互不相同；创建官在 Gateway 重启前后沿用同一会话，`npm run acceptance:governance-feishu` 总结果为 `PASS`。
- 创建官首次回复把“草案未来可申请的公开内容能力”误说成自己当前能读网页。Prompt 已明确区分当前工具与草案能力；重启后的原会话追问中，创建官正确记住“青松”，收回网页能力并只列出 `clarify`、`task_create`、`task_get/task_list` 等实际工具。
- 单独重配创建官时还发现 Hermes 虽写出 plist，却未保证 LaunchAgent 重新注册。配置器现在在官方安装后验证 `launchctl print`，缺失才 `bootstrap`，再统一 `enable + kickstart`；最终创建官由独立 LaunchAgent 守护。
- A君模型授权服务已删除首批员工硬编码白名单，改为从 active Manifest 的 `runtimeProfileRef` 和 Profile 映射动态解析；没有正式岗位或映射不一致时失败关闭。
- A君账号状态页直接调用小D既有通用连接管理的安全接口；只保留平台、账号别名、状态、允许员工/动作/范围、健康时间与“是否有受控凭据引用”，明确剔除凭据引用值、CookieBridge client ID、Cookie 和 Token。正式浏览器显示 6 张卡、3 个撤销入口且敏感输入为 0。

### 2026-07-26 本机三员工闭环

- 全量测试：A君运行时当前 `409/409`、小D `31/31`、Mac 工作间 `2/2`、岗位清单、GCP 安全部署、迁移与上线门禁合计 `34/34` 通过。
- 真实总任务：`a25fbd75-3068-43aa-b0bf-286d5f8a7084`，最终为 `succeeded / mission_delivered`。
- 小D：`cc8c894e-98fe-4f2e-bfb1-6bb382f64cc7`，公开示例视频处理完成，进度 100，`xiaod_media_delivery` 可读、非空、质量通过，飞书文档权限确认。
- 小R：`c63ee7ea-f079-447e-b2d3-2d86a057ba27`，公开网页研究完成，`intel_research_report` 可读、非空。
- 办公执行助理：`22eb3c21-c36a-4d65-9ad9-61dd47483f14`，在前两项结束后生成 `office_briefing_package`；汇报包核对 2 项前置工作，0 项未完成。
- 并行证据：小D与小R在同一秒启动；办公执行助理在前置任务结束后启动。
- A君统一回话：显示 3/3 完成、三位员工状态、统一汇报和下一步，不把任何部分完成冒充完整成功。
- 幂等证据：重复提交同一稳定请求引用前后均为 209 条任务，返回原总任务且不重新分派。
- 重启证据：A君运行台与 Hermes Gateway 均更换进程后，总任务及三条子任务仍为成功；A君原生模型通过 `task_get` 读到同一结果，待审批为 0。

### 2026-07-26 A君真实飞书三员工回归

- 第一次真实回归保留失败证据：总任务 `e5316239-ba54-4841-9991-199222c03c29` 中，B 站素材失败、研究缺材料，最终汇报又被模型误路由给小R；A君没有冒充完成，而是在原飞书会话报告重要异常。
- 修正最终汇报路由后，第二次回归暴露父任务改写问题：任务 `9061143d-63d8-4fef-9f97-90dd7b3389f2` 被标题中的“老板汇报”误改成单条办公任务，`sourceTasks` 为空。A君再次如实报告“子工作未执行”，没有把空汇报当成成功。
- 服务端随后保留 `army.cross-agent-mission` 控制面父任务，只对实际业务子任务做岗位规范化。总任务 `80e7ab0e-2e20-4693-b4f7-5c4db2561d2e` 真实创建小D与小R并行任务；办公执行助理在计划中保持 `planned`，前两项结束后才创建，最终为 `succeeded / mission_delivered`，三项产物均已验证。
- 为避免短任务停在中间回执，`mission_create` 会在受限时间内等待终态，并在办公子任务延迟创建后重新读取完整子任务集合。最终回执总任务 `feb3a64e-cf10-4d12-9bd9-0f7b6f39d633` 在真实飞书只返回一条最终汇报：小D、小R、办公执行助理均为 `succeeded`，来源 `3/3`，总任务 `3/3`。
- A君与 Hermes Gateway 随后都由 launchd 更换进程，任务账本保持 `236 → 236`、飞书入口保持 `ready`。在原飞书会话追问“继续刚才”后，A君命中同一总任务及三条原子任务，没有创建或重复执行任何任务。
- A君、Paperclip、小D、小D Hermes Gateway、小R Gateway、小办 Gateway 和默认 Hermes Gateway 的 launchd 服务均为已加载运行，`RunAtLoad=true`、`KeepAlive=true`；默认 Hermes 服务定义与当前安装一致。这验证了登录自启和崩溃拉起配置，但不是 Mac 真断电证据，关机后可用性仍归 EMP-010。

### 2026-07-26 高风险拒绝

- 验收任务 `be834ae7-8713-4373-9902-eb61fc0a9a03` 包含公开发布目标，只进入 `waiting_approval`，没有子任务和产物。
- Paperclip 审批拒绝后，总任务为 `cancelled / governance_rejected`，子任务仍为 0，待审批恢复为 0。

### 2026-07-26 混合在线与员工接线

- 私人云端与 Mac 工作间部署包位于 `ops/hybrid-online/`；云端仅回环监听，通过 Google IAP SSH 将 A君端口映射到老板 Mac 的回环端口，不增加第三方组网账号，也不公开暴露运行台。
- 隔离云端任务 `4c502961-1509-4cc8-876f-fcf38d6cde6a` 从 `waiting_worker` 被有效 Worker 租约领取；错误 Token 返回 401；心跳写回 `running / 55%`；完成后为 `succeeded`，并留下已验证的脱敏 `xiaod_media_delivery` 引用。
- 过期租约可被新 Worker 接管，旧租约不能覆盖新结果；没有可验证产物时不能写成成功。Mac Worker 以云端任务 ID 幂等创建小D工作，重启后仍能读取本机映射。
- 第二轮隔离云端验收不启动 Mac Worker：小R任务 `3d702211-b799-4856-b635-757738b7160f` 读取 1 条公开来源并生成结构完整、非空的 `intel_research_report`；办公助理任务 `1b2ca0e5-94fd-4b9d-ab7e-2cc9c9d19a5f` 基于该结果生成 913 字节汇报包，来源状态、未决事项和下一步门禁均通过。
- 同轮小D任务 `5538a602-f732-4d4d-a704-ec7b01f826da` 保持 `waiting_worker / waiting_for_mac_worker`，0 产物、0 错误。隔离云端进程完整停止并以同一数据目录重启后，三项任务状态和办公产物仍可读，当前重点仍指向等待 Mac 的任务；重复提交小R幂等键后任务数仍为 3、任务和产物 ID 均不变。
- 上线前体检覆盖云端回环监听、目录隔离、Hermes Profile、IAP SSH 回环映射、小D回环地址、Worker 令牌、文件权限和目标系统命令；GCP 计划另有独立网络、IAP-only SSH、Shielded VM、付费双门禁和失败停机验证。体检只输出检查名称，不输出配置值。
- 本机员工接线页已在正式 `4321` 运行时和真实浏览器复验：小R和办公执行助理各有一张最小权限卡；两张卡分别显示“模型待授权”和“飞书未接线”，不会再把选过模型误写成只差飞书；两个 App Secret 字段均为密码输入；六个输入初始为空；页面无 App ID、`open_id` 或 Secret 回显；浏览器错误/警告为 0。
- 两张卡均新增“打开模型授权”：后端仅白名单首批员工，复用 Hermes `dashboard`，固定 `127.0.0.1:9119`，并返回 `/env?profile=intel-researcher` 或 `/env?profile=office-assistant`。正式运行已验证 Dashboard 识别两个 Profile；真实浏览器进入小R链接时顶部明确显示 `Managing profile “intel-researcher”`。未执行登录、断开、模型切换或模型调用。
- 小R Manifest 已依据既有公开只读受限测试、负责人激活及真实飞书任务证据，从静态 `draft` 收敛为 `active`；独立 Profile 仍保持 `profile-created / credentialedTransportVerified=false`，不冒充飞书或模型入口已上线。

### 2026-07-26 小R与小办独立飞书闭环

- 经老板明确批准，使用飞书官方创建流程建立并发布“小R”和“小办·办公执行助理”两个独立应用；每个应用仅申请私聊消息读取、机器人发消息和 `im.message.receive_v1` 事件，不记录任何应用凭据或用户标识。
- 两个员工通道在正式 A君运行时均显示 `connected`；两个机器人分别只向老板发送一条上线验收问候。
- 小办从老板与该员工的真实私聊接到办公汇报任务 `24209377-f724-43e7-bc08-be06a80c372b`，任务以 `succeeded / office_briefing_ready` 结束，生成非空、可读的 `office_briefing_package`，并回到同一私聊。
- 小R前两次真实私聊任务安全停在 `needs_input`，暴露出中文标点被吞进公开 URL 的解析问题；修复后，回归覆盖“中文逗号不进入链接”，没有掩盖失败记录。
- 小R随后从同一真实私聊接到任务 `76457d00-13ef-40b3-bb20-0e4b26211aad`，准确读取 1 条 CERN 公开来源，以 `succeeded / intel_research_ready` 结束，生成结构完整、非空、可读的 `intel_research_report`，并把中文研究报告回到同一私聊。
- URL 修复后的 A君全量自动检查为 `379/379`；员工飞书唯一接管和云端预检额外覆盖通过。当前 Mac 正式归属保持 `local`，云端未启动，因此不存在双端同时接管。

### 2026-07-26 小R与小办迁移到独立 Hermes Gateway

- 两个独立 Profile 均通过 Hermes 官方模型配置选用 OpenAI Codex，并各自完成一次真实模型调用；映射状态收敛为 `verified-local`，不是从其他员工 Profile 继承出来的静态配置。
- 小R与小办的飞书最小配置由受控迁移器分别写入对应 Profile，文件权限为 `600`；迁移器只管理白名单字段、保留其他 Profile 配置，并拒绝首批名单外员工。仓库、日志和验收记录不保存任何凭据或用户/会话标识。
- 两个 Profile Gateway 均由 macOS 服务守护并真实连接飞书长连接；A君官方 SDK Fleet 对这两个应用显示 `external` 且不读取密钥、不启动第二条连接。其余员工入口与 A君现有能力不受影响。
- 小R在独立 Hermes 会话中完成真实研究任务 `ed28e80c-d6b1-4eb2-b963-5d0cff203d86`，调用岗位作用域的 Agent Army MCP 创建并读取任务，返回带来源的中文结论、证据和下一步。
- 小办在独立 Hermes 会话中完成真实办公任务 `ed255ef7-1767-4916-9fdc-032f8a44573f`，调用同一任务真相生成汇报、未决项和老板下一步。
- 随后分别在原私聊追问“基于刚才内容、不创建任务”的问题：小R准确引用此前研究与任务号，小办准确引用此前汇报中的云端关机验收未决项。追问前后任务总数保持 `216 → 216`，证明只读连续对话没有重复创建任务。
- 两个 Profile 均关闭飞书工具过程气泡、临时助手消息和详细忙碌回执，只保留必要的长任务通知与最终结果；真实追问只看到老板消息和员工最终回复，符合“幕后能力只在需要时显示”。
- 正式 `4321` 概览在重启后对两名员工均显示 `independentRuntime.ready`、`feishuChannel.external` 和 `verified=true`；运行台对已接管员工隐藏凭据修改表单。A君全量自动检查为 `384/384`，岗位映射 `7/7`，接管与迁移专项 `10/10`。

### 2026-07-26 GCP 私人云部署隔离验证

- Google Cloud 部署器默认只读；当前项目未启用结算时只报告资源缺失，不启用 API、不创建网络或实例。真正执行同时要求命令确认词和独立环境门禁。
- 创建实例前还必须成功建立只覆盖当前项目的 35 美元月度预算提醒，并在 50%、90%、100% 时通知；预算提醒不是自动硬停机，创建失败会阻止任何资源创建。
- 目标固定为 `us-central1-a` 的 `e2-medium`、40 GB 标准盘、独立 VPC；防火墙只允许 Google IAP 来源访问 SSH，A君和 Paperclip 端口均不对公网开放；VM 开启 Secure Boot、vTPM、完整性监控和删除保护，不附带服务账号。
- Ubuntu 24.04 `amd64` 隔离主机真实执行引导成功：Hermes 安装脚本固定提交并校验 SHA-256，网络失败具备重试和官方 GitHub 备用地址；Hermes 与固定版 Paperclip 均完成安装，未迁移任何凭据、未启动业务、未接管飞书。
- Mac 与云端改用 Google IAP SSH 回环映射，不再增加第三方组网账号。服务准备脚本默认不启用、不启动三套 Hermes Gateway、A君或 Paperclip。
- Paperclip 跨 macOS/Linux 迁移不复制原始 PostgreSQL 数据目录，改用官方 `db:backup` 可移植 SQL、IAP 加密传输和双门禁恢复；恢复失败会停止服务并保留诊断现场。
- 当前 Paperclip 真实数据库已完成一次不停止员工的官方备份探针，成功生成约 1.68 MiB 的 `.sql.gz` 可移植备份；没有执行恢复或修改任务。
- 迁移清单逐文件覆盖 A君任务、默认 Hermes、A君顾问、小R和小办的配置、授权、状态与原会话，覆盖飞书私有配置、Paperclip 配置/主密钥/附件和唯一 SQL 备份；缺文件、篡改、重复备份、符号链接和宽泛导入目录均失败关闭。
- 本机归档使用一次性 AES-256 密码并只把密码保存到 macOS 钥匙串；上传时在临时目录解密复验，经 IAP 传输。云端导入、服务准备和激活分离，导入不会提前启动 Gateway。
- 唯一接管状态机要求激活前再次确认本机五项服务停止；云端启动失败时先停云，只有能确认云端入口全部停止才恢复本机。首次激活后的即时回退脚本已准备，云端产生新任务后的反向同步仍需在真实主机阶段验证。

### 尚缺外部证据

- 两个独立飞书应用、模型调用、真实私聊业务闭环和连续追问已经完成；后续权限变化仍需老板单独确认，未擅自扩权。
- 隔离云端任务已证明桥接、无 Mac 时轻量员工执行和重启恢复有效；当前没有可用的常在线主机，现有 Google Cloud 项目也未启用结算，因此尚未证明真实私人云主机、IAP 隧道、Mac 真关机后的飞书收发或云端模型授权。
