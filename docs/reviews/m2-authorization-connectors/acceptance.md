# M2 授权连接与内容获取验收记录

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成：YouTube 与小红书读取闭环通过；A君正式登录、禁用和续期已完成自动化、本机 API、浏览器、外部平台与人工验证 |
| 日期 | 2026-07-27 |
| 验收范围 | YouTube 公开字幕读取、小红书受控只读、通用内容获取、小D转录整理、撤销与重新授权恢复 |
| 不包含 | 凭据、Cookie、令牌、视频内容、从零登录/OAuth 的人工输入与验证码 |

## 自动化与本地运行

- `apps/xiaod-media-transcriber npm test`：29/29 通过；`integrations/access/test/common-access.test.js`：11/11 通过。
- 小D服务仅监听 `127.0.0.1:4318`；健康检查报告通用内容获取中心和 `yt-dlp` 适配器为就绪。该健康状态不代表真实外部获取成功。

## P5 真实只读验证

- 所有者提供一条可合法读取的 YouTube 视频；任务未绑定账号连接，通用适配器直接取得公开视频字幕，并完成“读取字幕 → 语义整理 → 飞书文档交付”。产物为 605 个清洗后字符、1,101 个导览字符，飞书文档已创建并授予权限。
- 结论：公开字幕路径已获得真实闭环证据；`browser_session` 路径仍被拒绝。2026-07-24 已撤销此前为自研浏览器字幕读取创建的临时连接，并移除该读取器与登录入口；YouTube 公开视频统一复用 `yt-dlp`，不读取 Cookie。

## P6 小红书登录型只读验证

- 2026-07-27，所有者提供一条获准读取的低风险小红书素材；小D任务 `795b5e4a-1a5e-4d04-a79e-09898e6ebffd` 通过命名 `xhs` 连接进入 MediaCrawlerPro 深度通道，完成素材读取、本地 ASR、稿件生成和飞书文档交付。
- 所有者实际打开飞书文档，确认文档可访问且内容对应原素材。该证据证明的是具体任务真实成功，不以连接健康或自动化测试代替外部结果。
- 本次语义整理服务两次均在 30 秒后超时；任务如实保留告警，并交付完整校对文本、待人工确认导览和飞书文档，因此不把语义提纯记为完整成功。
- 安全核对发现，来源链接中的授权型查询参数没有进入受版本控制的 PRD、验收文档或源码，但被原样写入本机 `apps/xiaod-media-transcriber/data/jobs.json`。因此真实读取与恢复可以记为通过，敏感来源引用的持久化安全门禁不能记为通过。

## 撤销与恢复验证

- 已撤销本次临时 YouTube 连接；后续同一来源请求在开始素材获取前失败，提示“账号连接已撤销或停用，请重新授权”。
- 运维事件同时记录撤销和 `reauthorize` 恢复建议；未触发第二次外部媒体获取。
- 小红书首次读取并经所有者确认后，原命名连接被真实撤销；同源任务 `ee7306a0-7dd4-4364-9c55-203d86dd1f88` 在素材获取前进入 `needs_input`，未继续读取外部内容，运维事件记录 `connection_revoked` 和 `connection_unavailable`。
- 所有者随后在小D受控页面登记新命名连接“M2验收-重新授权”，未在聊天或表单中粘贴 Cookie、密码或 token；恢复任务 `fb19bca8-ac4c-467a-80c9-1958832779cf` 再次完成读取、转录、本地稿件和飞书文档交付，旧连接继续保持已撤销终态。

## A君正式登录入口、禁用与续期

- A君运行台已提供小红书、抖音、哔哩哔哩和快手的白名单登录入口，只允许本机老板调用；登录按钮通过固定 `/usr/bin/open -a "Google Chrome"` 打开平台官方页面，不接受任意 URL。
- A君只读取浏览器伴侣返回的已连接账号标识和脱敏昵称；创建与续期请求固定为小D、`content:read` 和三项只读媒体动作，页面、A君接口和小D安全响应均不包含 Cookie、Token、密码或内部凭据引用值。
- 正式 launchd 服务重启后，小红书连接经 A君 API 完成“暂时禁用 → 同一连接 ID 续期恢复”，状态从 `disabled` 回到 `active`，员工与动作范围未扩大；A君页面提交“续期并恢复连接”再次返回 HTTP 200。
- A君登录入口已真实返回 HTTP 200，并在 Google Chrome 打开小红书登录页。桌面与 390 像素手机宽度浏览器验证均无横向溢出，页面包含 4 个允许平台、已登录账号选择、续期/禁用/撤销操作和 0 个敏感输入框；刷新后控制台无错误。
- 自动化回归：A君 `411/411`、小D `31/31` 通过。
- 所有者随后从 A君正式入口完成实际登录与授权，A君出现新的活动连接“授权测试0727”；该连接只分配给小D并保持三项只读媒体动作。任务 `7c432d52-45f1-4334-b2e6-5fe6812d3bd6` 使用该新连接完成同一获准素材的真实读取、本地转录、稿件生成和飞书文档交付。
- 本次最终任务仍出现语义整理 30 秒超时并按设计降级为完整校对文本和待确认导览；登录、授权读取和飞书交付成功，语义提纯质量另行处理。

## 关闭结论

- M2 登录产品化和授权读取门禁均已关闭。
- 当前只有负责人单用户使用且服务只监听本机。负责人接受来源链接敏感参数可能原样保存在本机任务状态的已知风险，因此该项不阻塞 M2；进入多人、云端或远程访问前必须重新打开并完成安全改造。
- 语义整理服务连续三次超过 30 秒并触发安全降级；不影响本次授权验收，但仍是内容质量链路的独立问题。
