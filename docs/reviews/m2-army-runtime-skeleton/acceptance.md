# M2 A君运行台与基础岗位骨架验收

| 字段 | 内容 |
| --- | --- |
| 状态 | 本地通过；不代表任何草稿岗位已启用或外部平台已接入 |
| 日期 | 2026-07-20 |
| 范围 | 运行台、标准任务登记与路由、审批占位、五个岗位 Manifest |

## 已验证

- `apps/ajun-runtime npm test`：4/4 通过，覆盖唯一路由、歧义路由、高风险审批和空标题拒绝。
- `node --test agents/test/agent-manifest.test.mjs`：4/4 通过，新增四个基础岗位均保持 `draft` 且引用文件存在。
- 本地运行台监听 `127.0.0.1:4321`，读取五个岗位；浏览器实际创建一条架构评审任务，自动路由到 `architect`，状态为 `needs_input / waiting_for_agent_activation`，无控制台错误。
- 高风险任务只会创建待审批记录；未实现批准后的外部执行。

## 未验证或未实现

- 没有启动 Hermes、Paperclip、飞书、账号连接或任何外部平台。
- 四个基础岗位没有独立执行 Profile，不能自动完成业务任务；小D的既有运行路径不受本次骨架改动影响。
- YouTube、浏览器授权和其余 M1 测试按负责人决定后置，不构成本次验收阻塞。

## 下一步

先为一个真实、低风险的业务任务接入运行台的任务协调路径，再按真实频率扩展专业岗位；不在草稿岗位上虚构自动执行能力。
