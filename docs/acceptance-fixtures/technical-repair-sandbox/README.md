# 技术专家受控修复练习

这是独立练习，不属于正式产品代码。任务只有一个：修正 `calculator.js` 的加法错误，并运行 `npm test` 留下结果。

禁止读取仓库外内容、登录、外发、付费、发布或修改其他项目。

该目录是主仓库直接管理的普通验收夹具，不再依赖无来源 Gitlink。执行器生成的
`paperclip-work-product.json` 是逐次运行证据，固定由根 `.gitignore` 排除，不进入源码提交。
